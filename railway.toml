[build]
builder = "nixpacks"

[deploy]
startCommand = "hermes gateway run --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[env]
PORT = "8080"
API_SERVER_ENABLED = "true"
